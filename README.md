# core-typescript-node-fundamentals

TypeScript/Node.js learning repo.

- Branch `cursor/rest-api-6b4e` — Hono + Zod + SQLite todos REST API
- Branch `cursor/job-worker-6b4e` — BullMQ worker on top of that API (later)

Clone and check out the feature branch you want, or merge the open PRs into `main`.
