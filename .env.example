# Required: your Anthropic API key. Never commit the real .env file.
ANTHROPIC_API_KEY=

# Optional overrides (defaults shown). See LEARNING.md.
ANTHROPIC_MODEL=claude-sonnet-4-5

# Hard cap on acting LLM turns (planning is extra). Mandatory — the model will not stop itself.
AGENT_MAX_ITERATIONS=8
