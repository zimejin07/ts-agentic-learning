# SQLite file used by the API (gitignored). Tests use an in-memory database.
DATABASE_PATH=data/app.db
PORT=3000
# Required for enqueueing jobs and for `pnpm worker`. Start Redis with: docker compose up -d redis
REDIS_URL=redis://127.0.0.1:6379
